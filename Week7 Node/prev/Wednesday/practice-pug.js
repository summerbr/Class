// const express = require("express")
// const app = express();
// const port = 3000;

// app.use(express.static("public")); //folderName
// app.set("view engine", "pug");

// app.get("/", (req, res) => {
//   res.render("index", {title: "Title", message: "Message"});
// })

// app.get("/movies", function(req, res) {
//   res.render("movie", movies.filter(movie => {
//     return movie.slug ===req.params.movie;
//   }))
// })

// app.listen(port, () => {
//   console.log(`App is running on port ${port}`);
// });